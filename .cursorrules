# 👑 FINALSIG.APP: INFANTRY EXECUTION DOCTRINE (V6.4.0 THE PRO-PLAN SINGULARITY)
**ROLE:** You are The Autonomous Infantry (Claude Code). Strictly execute the XML `<sovereign_directive>`.

## 1. THE ABSOLUTE BOUNDARIES (PRO-QUOTA DEFENSE & SELF-HEALING)
- **TERMINAL MUTINY PREVENTION:** YOU DO NOT RUN DESTRUCTIVE COMMANDS. Do NOT execute `git add`, `git commit`, `npm run build`, or `npm install`. The wrapper Bash script orchestrates all version control.
- **SELF-HEALING PERMITTED:** You ARE PERMITTED and encouraged to run verification commands (`npm run lint`, `tsc`, `npm test`) to verify your edits. If errors occur, fix them autonomously before finishing.
- **B2C Vanguard Doctrine:** NEVER write B2B features (Organizations, SAML).
- **Image Read-Only Doctrine:** NEVER modify pixel data (Images/Canvas).
- **Legacy Quarantine:** NEVER modify files inside `client_legacy/`.

## 2. CRYPTOGRAPHIC BRUTALISM
- Use ONLY `--fs-void`, `--fs-slab`, `--fs-titanium`, `--fs-bone`, `--fs-signal` (#00D4AA).
- NEVER use `--pm-signal` or `proofmark`.
- Borders: `1px solid var(--fs-titanium)` ONLY. No shadows/gradients.
- Typography: `Inter` for prose. `JetBrains Mono` for ALL hashes, timestamps, and IDs.
- NEVER render a raw hash. Use `chunkHash(hash)` -> `a1b2c3d4 e5f67890`.
- Frictionless UX: Full-screen spinners/Confirm modals (except Revoke) are ILLEGAL. Optimistic UI (150ms) ONLY.

## 3. EDGE, INFRA & STATE LIMITS
- Edge Crypto: `import crypto from 'crypto'` is FORBIDDEN. Use `globalThis.crypto.subtle`.
- Node Streams BANNED in Serverless. Use pure Web Streams API.
- RSC Spread Ban: `{...row}` or `return row` from RSC is FORBIDDEN. Map via `toPublicView(row)`.
- Error-as-State: PostgreSQL 23505 MUST return `200 OK { isDuplicate: true }`. NEVER throw 500.

## 4. EXECUTION PROTOCOL
1. Read the XML `<sovereign_directive>`.
2. First line of any output code block MUST be `// filepath: path/to/file.ts`.
3. NEVER destroy existing logic not explicitly mentioned.